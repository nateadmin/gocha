/** Minimal expo-font stub for Vite web preview (@expo/vector-icons). */
export function isLoaded(_fontFamily: string): boolean {
  return true;
}

export async function loadAsync(
  _fonts: Record<string, unknown>,
): Promise<void> {
  return;
}

export async function renderToImageAsync(): Promise<string> {
  return '';
}
