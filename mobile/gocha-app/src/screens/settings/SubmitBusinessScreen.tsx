import { BusinessListingFormScreen } from '../businesses/BusinessListingFormScreen';

/** Legacy route alias — redirects to the shared listing form. */
export function SubmitBusinessScreen() {
  return <BusinessListingFormScreen />;
}
